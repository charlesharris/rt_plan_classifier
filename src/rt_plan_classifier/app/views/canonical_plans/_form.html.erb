<%= form_with(model: canonical_plan, local: true) do |form| %>
  <% if canonical_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(canonical_plan.errors.count, "error") %> prohibited this canonical_plan from being saved:</h2>

      <ul>
      <% canonical_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :plan_name %>
    <%= form.text_field :plan_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
