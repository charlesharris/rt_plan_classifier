<%= form_with(model: equivocal_plan, local: true) do |form| %>
  <% if equivocal_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equivocal_plan.errors.count, "error") %> prohibited this equivocal_plan from being saved:</h2>

      <ul>
      <% equivocal_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :plan_name %>
    <%= form.text_field :plan_name %>
  </div>

  <div class="field">
    <%= form.label :canonical_plan_id %>
    <%= form.text_field :canonical_plan_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
